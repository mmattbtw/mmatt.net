generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model posts {
  id       String @id
  category String
  imageUrl String
  markdown String
  slug     String
  title    String
}
