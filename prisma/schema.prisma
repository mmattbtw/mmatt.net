generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("MONGO_URI")
}

model posts {
  id       String @id @default(auto()) @map("_id") @db.ObjectId
  category String
  imageUrl String
  markdown String
  slug     String
  title    String
}
